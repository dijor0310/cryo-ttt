# exp setting
exp_name: it^3-try # name that will be used in wandb
# ckpt_path: /workspaces/cryo/cryo-ttt/src/ttt_ckpt/segment-sanity-b10-bn-2c296/epoch=108-val/dice_loss=0.36.ckpt #/ checkpoint path, used for evaluation
ckpt_path: null
seed: 42 # random seed
debug: False # debug mode, will use cpu only
devices: [ 1 ] # gpu ids

# data related
load_num_workers: 63 # number of workers for loading data

# (wandb) logging
log_every_n_steps: 1

defaults:
  - _self_
  - method: deepict_unet3d_ittt